Steps:
  - Download the zip.
  - Extract the files into the local directory (I have used Xampp for localserver).
  - Create a new local Database called square1
  - Configure the .env archive and insert the username and password
  - Open Terminal into the directory and make the migrate.
  - Finally open de browser and execute.
